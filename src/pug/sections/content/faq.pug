extends ../../layout/content-block
include ../../modules/mixins

block variables
    - sectionClass = "trustpilot"
    - contentTitle = "trustpilot"
    - linkContent = "check all faq"

block headerLink
        +link(`${linkContent}`, '#', true)

block content
    